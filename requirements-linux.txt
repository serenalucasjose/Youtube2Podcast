# Dependencias adicionales para Linux/Raspberry Pi
# Incluye paquetes optimizados para CPU/ARM
-r requirements-base.txt

# STT optimizado para CPU
faster-whisper

# TTS offline (requiere onnxruntime)
# NOTA: En ARM (Raspberry Pi), piper-tts se instala por separado
# en install_dependencies.sh para manejar problemas de compatibilidad
# Si estás instalando manualmente en x86_64:
#   pip install piper-tts
# En ARM, el script intentará instalarlo con manejo de errores apropiado
