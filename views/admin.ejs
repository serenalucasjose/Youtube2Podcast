<!DOCTYPE html>
<html lang="es" class="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin - Youtube2Podcast</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/custom.css">
</head>
<body class="bg-gray-900 text-gray-100 min-h-screen flex flex-col">

    <nav class="bg-gray-800 shadow-lg">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16 items-center">
                <div class="flex items-center">
                    <a href="/" class="text-2xl font-bold text-red-500 hover:text-red-400 transition">Youtube2Podcast</a>
                    <span class="ml-4 text-gray-400 text-sm">/ Admin Panel</span>
                </div>
                <div class="flex items-center">
                    <a href="/logout" class="text-gray-400 hover:text-white">Salir</a>
                </div>
            </div>
        </div>
    </nav>

    <main class="flex-grow container mx-auto px-4 py-12 grid grid-cols-1 lg:grid-cols-2 gap-8">
        
        <!-- User Management -->
        <div class="space-y-8">
            <div class="admin-section">
                <h2 class="text-xl font-bold mb-6 text-white border-b border-gray-700 pb-2">Usuarios</h2>
                
                <!-- Add User Form -->
                <form action="/admin/users/add" method="POST" class="mb-8 bg-gray-900 p-8 sm:p-10 rounded-lg border border-gray-700 shadow-inner">
                    <h3 class="text-sm font-bold text-gray-400 mb-6 uppercase tracking-wide border-b border-gray-800 pb-2">Crear Nuevo Usuario</h3>
                    <div class="grid grid-cols-1 md:grid-cols-4 gap-6 items-end">
                        <div class="w-full">
                            <label class="block text-xs text-gray-500 mb-2 uppercase font-bold tracking-wider">Usuario</label>
                            <input type="text" name="username" placeholder="Ej. pepe" required 
                                class="px-4 py-2 rounded bg-gray-800 border border-gray-700 text-white focus:border-red-500 outline-none w-full transition h-12 text-sm focus:ring-1 focus:ring-red-500/50">
                        </div>
                        <div class="w-full">
                            <label class="block text-xs text-gray-500 mb-2 uppercase font-bold tracking-wider">Contraseña</label>
                            <input type="password" name="password" placeholder="******" required 
                                class="px-4 py-2 rounded bg-gray-800 border border-gray-700 text-white focus:border-red-500 outline-none w-full transition h-12 text-sm focus:ring-1 focus:ring-red-500/50">
                        </div>
                        <div class="w-full">
                            <label class="block text-xs text-gray-500 mb-2 uppercase font-bold tracking-wider">Rol</label>
                            <select name="role" class="px-4 py-2 rounded bg-gray-800 border border-gray-700 text-white focus:border-red-500 outline-none w-full transition h-12 text-sm focus:ring-1 focus:ring-red-500/50">
                                <option value="user">User</option>
                                <option value="admin">Admin</option>
                            </select>
                        </div>
                        <div class="w-full">
                            <button type="submit" class="bg-blue-600 hover:bg-blue-700 text-white py-2 rounded-lg transition w-full font-bold shadow-lg h-12 text-sm uppercase tracking-wider transform hover:translate-y-[-1px]">
                                Crear
                            </button>
                        </div>
                    </div>
                </form>

                <!-- User List -->
                <div class="admin-table-container">
                    <table class="admin-table">
                        <thead>
                            <tr>
                                <th>Usuario</th>
                                <th>Rol</th>
                                <th>Nueva Pass</th>
                                <th>Acciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% users.forEach(u => { %>
                                <tr>
                                    <form action="/admin/users/update/<%= u.id %>" method="POST">
                                        <td class="px-4 py-3">
                                            <input type="text" name="username" value="<%= u.username %>" class="inline-input" required>
                                        </td>
                                        <td class="px-4 py-3">
                                            <select name="role" class="inline-input">
                                                <option value="user" <%= u.role === 'user' ? 'selected' : '' %>>User</option>
                                                <option value="admin" <%= u.role === 'admin' ? 'selected' : '' %>>Admin</option>
                                            </select>
                                        </td>
                                        <td class="px-4 py-3">
                                            <input type="password" name="password" placeholder="Nueva..." class="inline-input text-gray-500 focus:text-white placeholder-gray-600">
                                        </td>
                                        <td class="px-4 py-3 flex items-center gap-2">
                                            <button type="submit" class="text-blue-400 hover:text-blue-300 transition" title="Guardar cambios">
                                                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path></svg>
                                            </button>
                                            
                                            <% if (u.username !== 'admin' && u.id !== user.id) { %>
                                                <!-- Separate form for delete to avoid nesting issues -->
                                                <button type="button" onclick="if(confirm('¿Eliminar <%= u.username %>?')) document.getElementById('delete-form-<%= u.id %>').submit();" class="text-red-400 hover:text-red-300 transition" title="Eliminar">
                                                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path></svg>
                                                </button>
                                            <% } %>
                                        </td>
                                    </form>
                                    
                                    <!-- Hidden Delete Form -->
                                    <form id="delete-form-<%= u.id %>" action="/admin/users/delete/<%= u.id %>" method="POST" style="display:none;"></form>
                                </tr>
                            <% }); %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

        <!-- Danger Zone -->
        <div>
            <div class="admin-section border-red-900/30 bg-gray-800/50 sticky top-8">
                <h2 class="text-2xl font-bold mb-6 text-white border-b border-gray-700 pb-2">Zona de Peligro</h2>
                
                <p class="text-gray-400 mb-8">
                    Estas acciones son irreversibles y afectarán a todos los usuarios.
                </p>

                <div class="space-y-4">
                    <form action="/api/clear-all" method="POST" onsubmit="return confirm('¿ESTÁS TOTALMENTE SEGURO? Se borrarán TODOS los episodios y archivos descargados.');">
                        <button type="submit" class="w-full bg-red-900 hover:bg-red-800 text-red-100 font-bold py-4 px-6 rounded-lg transition duration-300 flex items-center justify-center gap-3 border border-red-800 shadow-lg">
                            <svg class="w-6 h-6 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path></svg>
                            Eliminar TODOS los Podcasts
                        </button>
                    </form>
                </div>
            </div>
        </div>

    </main>

</body>
</html>
